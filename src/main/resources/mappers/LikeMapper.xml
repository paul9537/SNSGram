<?xml version="1.0" encoding="UTF-8"?>

  <!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.paul9537.Gram.post.dao.LikeDAO">
	<insert	id="insertLike" parameterType="int">
		INSERT INTO `like` (
			`postId`
			, `userId`
			, `createdAt`
		) VALUE (
			#{postId}
			, #{userId}
			, now()
		)
	</insert>

	<select id="selectLikeCount" parameterType="int" resultType="int">
		SELECT
			count(*)
		FROM
			`like`
		WHERE
			`postId` = #{postId}
	</select>
</mapper>